  Libraries used to compile this projects.
  taken from "Documents/Arduino/libraries".
  These were downloaded from git repositories.
  Some might be missing because the library was located 
  in "program files". 